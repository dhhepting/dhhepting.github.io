function setup() {
  createCanvas(800,600);
  // set a background colour to ensure that the canvas is visible
  // -> https://p5js.org/reference/#/p5/background
  // a single number is a greyscale value: 0 is black and 255 is white
  background(204);
}

function draw() {
  // put drawing code here
}
