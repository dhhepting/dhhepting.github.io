# are they in your ORIGINAL repo's working tree (the un-rewritten one)?
find ~/Sites/dhhepting.github.io -name '*.m4a' 2>/dev/null
# or already in an archive?
find ~/Sites/hepting-website -name '*.m4a' 2>/dev/null
